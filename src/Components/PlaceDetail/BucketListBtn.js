import { Button } from "@material-ui/core";

const BucketListBtn = ({ placeId }) => {
  return (
    <Button color="secondary" size="medium" variant="outlined">Add to Bucketlist</Button>
  );
};

export default BucketListBtn;
